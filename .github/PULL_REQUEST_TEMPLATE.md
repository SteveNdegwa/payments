## Context

<!-- One short paragraph: what does this PR add, remove, or change, and why?
     Link the ticket (e.g. SPIN-###) if there is one. -->

## Changes Made

<!-- The concrete changes. Tick each box as it lands. -->
- [ ] Change 1
- [ ] Change 2

## Testing / Validation

<!-- How you verified this works: commands run + their result, manual checks.
     At minimum: `make check` (ruff lint + format + tests). Note any migration
     applied, provider sandbox call made, or Celery task exercised. -->
- `make check` →
- Note 1

<!-- Optional, add only if relevant:
     ## Migrations        (list new migration files; note any data backfill)
     ## Deploy notes      (new env var / secret, Helm value, provider config)
     ## Security          (new gateway-exempt path, auth change, secret handling)

     Do NOT add any "Generated with Claude Code" / claude.ai/code session-link
     footer. The commit co-author trailer is fine; the PR footer is not. -->
